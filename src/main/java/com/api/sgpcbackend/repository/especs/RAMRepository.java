package com.api.sgpcbackend.repository.especs;

import com.api.sgpcbackend.domain.model.patrimonio.computador.espec.RAM;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RAMRepository extends JpaRepository<RAM,Short> {
}
