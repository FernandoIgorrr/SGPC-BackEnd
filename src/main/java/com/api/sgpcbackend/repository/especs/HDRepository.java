package com.api.sgpcbackend.repository.especs;

import com.api.sgpcbackend.domain.model.patrimonio.computador.espec.HD;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HDRepository extends JpaRepository<HD,Short> {
}
